<%= form_with(model: [datespot, comment], url: datespot_comments_path(@datespot) ) do |f| %>
  <%= f.text_area :content, class: "input-mysize" %>
  <%= f.submit "送信", class: "btn btn-outline-dark comment-submit float-right" %>
<% end %>
