<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :followed_id, @user.id %></div>
  <button type="submit" class="follow-button__before">
    <i class="far fa-thumbs-up follow-button__before--icon"></i>いいね！
  </button>
<% end %>
