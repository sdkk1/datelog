<%= link_to "/favorites/#{@datespot.id}/create", method: :post, class: 'like', remote: true do %>
  <div class="like-btn-like"><i class="far fa-heart"></i></div>
<% end %>
<span><%= @datespot.favorites.count %></span>
